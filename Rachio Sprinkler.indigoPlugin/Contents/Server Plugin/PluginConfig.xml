<?xml version="1.0"?>
<PluginConfig>
    <Field id="topLabel" type="label">
        <Label>To use the Rachio Plugin, you need to enter your API token, which is found in the Rachio web app (https://app.rach.io/).</Label>
    </Field>
    <Field id="accessToken" type="textfield">
        <Label>API token:</Label>
    </Field>
    <Field id="pollingInterval" type="textfield" defaultValue="3">
        <Label>Polling interval:</Label>
    </Field>
	<Field id="pollingIntervalLabel" type="label" fontSize="small" fontColor="darkgray" alignWithControl="true">
		<Label>Number of minutes to wait between polling Rachio for the status of your controller. 3 minutes is the minimum, but you may need to make it larger if you have multiple controllers to avoid Rachio rate limits.</Label>
	</Field>
    <Field id="apiCallTimeout" type="textfield" defaultValue="5">
        <Label>API call timeout:</Label>
    </Field>
	<Field id="apiCallTimeoutLabel" type="label" fontSize="small" fontColor="darkgray" alignWithControl="true">
		<Label>Number of seconds to wait for the API to reply before continuing. You probably don't need to adjust this unless instructed by support.</Label>
	</Field>
    <Field id="sep_statusscope" type="separator"/>
    <Field id="statusRequestScope" type="menu" defaultValue="all">
        <Label>On API call, refresh:</Label>
        <List>
            <Option value="all">All states</Option>
            <Option value="controller">Only Rachio Controller states</Option>
        </List>
    </Field>
    <Field id="statusRequestScopeLabel" type="label" fontSize="small" fontColor="darkgray" alignWithControl="true">
        <Label>What a status-request (RequestStatus) API call refreshes. "All states" is the original behavior (account, every controller's schedule, and weather). "Only Rachio Controller states" fetches just the requested controller's current schedule/active zone — far fewer Rachio API calls when something polls status frequently (e.g. a flow monitor requesting status every 30s while water flows). The 3-minute background poll still refreshes everything.</Label>
    </Field>
    <Field id="sep3" type="separator"/>
    <Field id="maxZoneRunTime" type="textfield" defaultValue="10800">
        <Label>Max Run Time per Zone</Label>
    </Field>
    <Field id="sep4" type="separator"/>
    <Field id="useWebhooks" type="checkbox" defaultValue="false">
        <Label>Use Rachio API Webhooks</Label>
    </Field>
    <Field id="useWebhooksNote" type="label" fontSize="small" fontColor="darkgray">
        <Label>Requires Indigo Reflector.</Label>
    </Field>
   <Field id="reflector_api_key" type="textfield">
        <Label>Reflector REST API key:</Label>
    </Field>
   <Field id="reflector_Note" type="label" fontSize="small" fontColor="darkgray">
        <Label>See https://www.indigodomo.com/account/authorizations</Label>
    </Field>
 </PluginConfig>
